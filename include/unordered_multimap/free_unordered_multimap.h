#ifndef FREE_UNORDERED_MULTIMAP_H
#define FREE_UNORDERED_MULTIMAP_H

#include "unordered_multimap/base_unordered_multimap.h"

void clear_unordered_multimap(UnorderedMultimap *map);
void free_unordered_multimap(UnorderedMultimap *map);

#endif
