#ifndef OPERATOR_UNORDERED_MULTIMAP_H
#define OPERATOR_UNORDERED_MULTIMAP_H

#include "unordered_multimap/base_unordered_multimap.h"

int unordered_multimap_size(UnorderedMultimap *map);
int unordered_multimap_is_empty(UnorderedMultimap *map);

#endif
